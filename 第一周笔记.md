### 第一周笔记

> ***前在寒假已经进行了一部分，所以笔记不是涵盖第一周所有小组任务的*** 



#### 星期二 26.3.7

浏览了第一周小组任务，完成了以下任务：

* 学习了python基本语法，学到了列表，元组，字典，函数

* 复习了markdown语法

* 检查安装**Pycharm、Anaconda、Jupyter**等工具

  

  > 附练习代码（全部注释了）：
  >
  > ```python
  > # print("hello,world")
  > #number=[1,2,4,7,5]
  > """
  >  if 1 in number:
  >     print("在")
  > 
  > if 9 in number:
  >     print("在")
  > else:
  >     print("不在")
  > """
  > # print(number[3])
  > # print(max(number))
  > # number.sort()
  > # print(number)
  > # print(list(reversed(number)))
  > '''
  > number.append(6)
  > number.insert(2,3)
  > print(number)
  > '''
  > 
  > 
  > # print(number.index(7,2,5))
  > # 元组的解包（拆包）
  > '''
  > t = (1, 4, 5)
  > x, y, z = t
  > print(x, z, y)
  > '''
  > 
  > '''
  > information ={"name":"chenjunming","age": 19}
  > information["name"] = "shujiahuhudashui"
  > print(information["name"])
  > print(information["age"])
  > # print(information.get("nam"))
  > # print(information.popitem())
  > print(information.keys())
  > print(information.values())
  > print(information.items())
  > 
  > for key in information:
  >     print(key,information[key])
  > for key, value in information.items():
  >     print(key,value)
  > if "name" in information:
  >     print("存在")
  > '''
  > 
  > # def cifang(a,b):
  > 
  >   #  return a**b
  > # result=cifang(a=2,b=3)
  > # print(result)
  > '''
  > def greet(name, msg):
  >     print(f"{msg}, {name}")
  > 
  > greet("Alice", "Hello")  # 正确
  > # greet("Alice")         # 错误，缺少一个参数
  > '''
  > 
  > x = 10  # 全局变量
  > '''
  > def func():
  >     global x
  >     x = 20  # 修改全局变量
  >     y = 5   # 局部变量
  >     print(x, y)
  > 
  > func()
  > print(x)  # 20
  > '''
  > '''
  > add =lambda a,b: a*b
  > a = int(input("请输入a" ))
  > b = int(input("请输入b" ))
  > print(add(a,b))
  > '''
  > ```

> **我觉得写md语法时直接用操作栏很快啊**



#### 星期三 26.3.7

> 课好多..

今天完成了以下任务：

* 学习了python中的模块和包
* python文件操作
* python的异常处理
* 面向对象初步

> 附部分练习代码（全部注释了）：
>
> ```python
> #模块和包
> '''
> import math
> print(math.sqrt(16))
> import test
> '''
> ```

> 与AI的部分对话：
>
> ![1773898817429](C:\Users\epson\AppData\Roaming\Typora\typora-user-images\1773898817429.png)

但是要说的是，今天后面学的知识我只是学了个大概，我觉得不如前一天扎实，后面会再巩固



#### 星期四 26.3.8

今天完成了以下任务：

* 基本学习完了python基础

* 大致了解了三大库语法**numpy、pandas、matplotlib**，大致学习了**json库** 

  > 好难，有点学不懂



#### 星期五 26.3.8

今天完成了以下任务：

* 深入学习了json库
* 学会使用git版本控制，分支控制
* 创建并关联了github仓库（之前用的是gitee）



#### 星期六 26.3.9

今天完成了以下任务：

* 开始尝试写这个作业了！

过程逼逼赖赖：

> woc我还没学完“矩阵运算，矩阵与向量的关系，线性变换，点乘、叉乘，矩阵求导“，ai给的代码好多看不懂啊
>
> ai说：首先确认一下：你之前已经学完了 Python 基础（变量、数据类型、流程控制、列表、字典、函数、文件操作、异常处理、面向对象初步），并且对 JSON 和 API 有初步了解。这个作业确实需要用到一些**线性代数**的知识（向量、坐标系变换等），不过数学部分我会帮你梳理清楚，你只需要把数学公式翻译成 Python 代码即可。因此，**你有能力完成这个作业**，只是需要一些指导。
>
> 表示怀疑
>
> 坐标系投影是什么东西
>
> 我去 坐标系体积
>
> 原来numpy在这里用到了
>
> 列表可以这么来表示坐标系欸
>
> 和ai博弈中..
>
> wc我傻逼了，，，，
>
> 正在把自己的注释融入进去，试着加上自己的理解hh
>
> 哎呦类是什么又忘记了
>
> **坐标系转移** 这一大段我怎么看不懂啊
>
> 我操了要用到numpy这么多函数，这没ai辅助怎么可能一个人学完并记住到最后完成
>
> 我现在迷糊了，这几个功能代码我不能全看懂。。
>
> 我说我怎么没有找到shape的定义 原来`shape` 是 numpy 数组的属性，能返回一个元组，表示数组的维度
>
> 线性代数没学到这里，还不太能理解模块里的数学逻辑啊
>
> 我操
>
> 到文件操作了，这一部分我也不是很熟
>
> 来用群里的数据试试
>
> 咦？json文件里不能用注释吗
>
> 666 还真不能啊
>
> 主函数不会写..
>
> 主函数看不懂
>
> 怎么运行失败了我操
>
> “不能直接用 `:.6f` 格式化。我们需要将 `angle` 转换为 Python 的浮点数“
>
> 哦运行成功了..
>
> 吗的我是废物

附终端运行结果截图：

![1774086886554](C:\Users\epson\AppData\Roaming\Typora\typora-user-images\1774086886554.png)



**ppt思考题**



还没学 “图论与多智能体系统一致性的理论基础“ 嘿嘿

> 先交了再说



### 周记

**这几周都太多课了，每天其实只有晚上利用一点时间来学习和完成任务，不过还是学了很多，进步了很多！很开心**

**难以避免的是确确实实没有办法一下子学的很不错，也比较依靠ai来完成其中的一部分代码，有一些知识其实学的不是很精，比如文件操作，json库的使用，numpy库的使用，还有一些知识没学，比如图论，矩阵求导等等**

**不过后面找时间会一一补上**

**总得来说这周很充实~~复习和学了很多知识   下周继续努力~💪**











